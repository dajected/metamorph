{
  "name": "metamorph",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check src/engine.js",
    "test": "node test/engine.test.mjs",
    "verify": "node --check index.js && node --check src/engine.js && node test/engine.test.mjs"
  }
}
